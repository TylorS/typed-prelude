import { join } from 'path'

export const TEST_ID_TO_METADATA_MAP = `testIdToMetadataId`

export const ROOT_DIRECTORY = join(__dirname, '..')
export const TESTS_DIRECTORY = join(ROOT_DIRECTORY, 'tests')
export const RESULTS_DIRECTORY = join(ROOT_DIRECTORY, 'results')
export const RUN_TESTS_DIRECTORY = join(ROOT_DIRECTORY, 'runTests')
export const LOGGING_DIRECTORY = join(ROOT_DIRECTORY, 'logging')
