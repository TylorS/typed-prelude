{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", "*.test.ts", "*.browser-test.ts"]
    },
    "init": {
      "exact": true
    }
  }
}
